# Merged Architecture Authority

Use `IMPORT_MATRIX.md` in `D:/Merge project` for import decisions. In this repo, the authority split is:

- `projectx/` governs runtime continuity, control, and evaluation.
- `pg_pipeline/` governs typed scientific records and lifecycle.
- `exact_work/` preserves imported mathematical authority and artifacts.
- `archive/` preserves source identity without making old material canonical.

The first success criterion is one complete vertical slice, not broad family coverage.

