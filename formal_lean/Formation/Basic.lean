import Mathlib

namespace Formation

/--
Formation-medium formal layer.

Status boundary:
These declarations are scaffolding only until source-bound definitions and
checked theorem statements are added. Aristotle/Ax-Prover outputs are candidates
only; Lean `lake build` is the checker.
-/
def VersionTag : String := "formation-lean-v0.1.0"

end Formation
