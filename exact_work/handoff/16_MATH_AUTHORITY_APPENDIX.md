# Math authority appendix

This file preserves the strongest explicit mathematical spine that was under-carried in earlier handoffs.

## A. Strongest explicit top-interaction representative formulas [M-EXACT]

These are the representative formulas that should be restated verbatim in any serious restart.

### A-branch
For the exact positive-Grassmannian finite top-cell branch `Gr_+(2,n)` (and dual `Gr_+(n-2,n)`):

`[μ_{Gr_+(2,n)}] = n·u_{n-4}`

where `u_{n-4}` is the positive generator of `H^{n-4}(S^{n-4}, Z)`.

### D4 branch
For `Gr_+(3,6)`:

`[μ_{Gr_+(3,6)}] = 4·u_3·Ω_D`

where `Ω_D` is the triality-invariant `D_4` coefficient direction.

### E6 branch
For `Gr_+(3,7)` (and dual `Gr_+(4,7)`):

`[μ_{Gr_+(3,7)}] = u_5(7·ω_A + 14·ω_D)`

where:
- `ω_A` is the fixed-node support direction,
- `ω_D` is the paired-node support direction,
- `u_5` is the positive generator of `H^5(S^5, Z)`.

## B. The exact theorem spine [M-EXACT]

### B1. Static residue base
`Gr_+(0,n)` is the terminal residue atom with `Ω=1`.

`Gr_+(1,n)` is the simplex/log-residue recursion base.

### B2. First faithful layer
The first faithful semantic layer is the intrinsic root-bundle `V` of oriented codim-1 residue-family lines.

### B3. Local transport law
The local surviving transport law is the `A_2` quarter-turn:
- `J_i(f_i)=f_{i+1}`
- `J_i(f_{i+1})=-f_i`
- `J_i(f_j)=f_j` for `j ≠ i,i+1`

### B4. Support-node orbit law
Top-interaction coefficient of support component represented by deleted node `i`:
- paired support node → `h+2`
- fixed support node → `(h+2)/2`

### B5. Parabolic residue recursion
For a fully supporting facet `Y` of parabolic type `Γ'`:

`Res_Y(μ_Γ)=±μ_{Γ'}`

This is the recursion that turns the solved branch into a theory rather than isolated coefficient facts.

## C. Surfaceology as the next exact frontier [U-EXACT + OPEN]

Surfaceology currently contributes an exact local finite baseline but not a solved global family footing.

Exact baseline preserved:
- `curve word C -> matrix M_C -> F_C -> u_C -> α_C`
- exact prepend `eL` and `eR` updates
- determinant scales by new edge weight under both
- `u`-update independent of the new edge weight under both

Authority statement:
- surfaceology is the **next exact frontier**
- it is the next place where the currently extracted structure must either survive or fail outside the present finite positive-Grassmannian top-cell closure

## D. Exact vs generated firewall

### Exact enough to use downstream now
- `Gr_+(0,n)` / `Gr_+(1,n)` static residue base
- intrinsic root-bundle layer
- local `A_2` quarter-turn transport
- finite positive-Grassmannian top-cell closure on `A`, `D_4`, `E_6`
- support-node orbit law
- parabolic residue recursion
- surfaceology local finite baseline

### Generated only
- additive/formal branches such as ACBM / RCSS
- abstract `D_n` continuation beyond exact `D_4`
- CRBSM and any realization class generated from the extracted constraints

### Provisional bridge only
- support survival
- boundary-primary disclosure
- apparatus coupling
- persistent relay/support ledger (suspect)
