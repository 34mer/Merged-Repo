# Quiver / Grouped-Carrier Source Intake Plan

STATUS: GENERATED / OPEN / SOURCE_NEEDED  
ARGUMENT_STATUS: PRIOR_CONVERSATION_ORE / GENERATED  
CHECK_STATUS: NOT_CHECKED  
SOURCE_STATUS: NOT_SOURCE_EXTRACTED  
PROMOTION_STATUS: BLOCKED  
TARGET_LOCATION: research_ledger/  
DO_NOT_PLACE_IN: SOURCE_EXTRACTED_CLAIMS.json

## 0. Non-Promotion Notice

This is a source-intake plan, not a source-extracted claim.

It does not assert a theorem, does not identify a physical substrate, does not certify engineering feasibility, does not solve R14, and does not promote quiver/grouped-carrier material into Formation Medium truth.

Inputs:

```text
research_ledger/PREVIOUS_GPT_QUIVER_GROUPED_CARRIER_SYNTHESIS_001.md
research_ledger/PREVIOUS_GPT_SYMBOL_RECOVERY_SYNTHESIS_001.md
research_ledger/R1_R14_CLASS_MATRIX_CANDIDATE.json
research_ledger/FORMATION_MEDIUM_CANDIDATE_VOCABULARY.md
```

## 1. Purpose

Prior GPT mining found a large quiver/grouped-carrier vein:

```text
subobjects / subrepresentations
  -> closure / admissibility
  -> inclusion / refinement order
  -> F-polynomial / Hall-Euler / factorization readout
  -> equality-layer / realization-class comparison
```

This plan defines how to source-bind that vein without polluting the repo.

## 2. Source Target Areas

### QSI-T1 — Quiver Subobjects / Subrepresentations

Question:

> What source material defines quiver representations, subrepresentations, and subobject posets in the way needed for carrier/admissibility analysis?

Desired extraction targets:

```text
subrepresentation definition
subobject poset / inclusion order
closure or admissibility under subobjects
quiver Grassmannian relation, if relevant
```

Formation contacts:

```text
S1
FM1-C1
FM1-C2
R9
R10
R14
```

Status:

```text
SOURCE_NEEDED
```

Forbidden inference:

```text
quiver subobjects are the physical substrate
```

---

### QSI-T2 — F-Polynomials / Cluster-Character Readout

Question:

> Can F-polynomials, cluster characters, or related quiver invariants function as source-side examples of intrinsic readout from carrier/admissibility data?

Desired extraction targets:

```text
F-polynomial definition
cluster character / Caldero-Chapoton-like formula, if used
quiver Grassmannian Euler characteristic terms, if used
invariant/readout interpretation limits
```

Formation contacts:

```text
S2*
FM1-C4
R7
R11
R12
```

Status:

```text
SOURCE_NEEDED
```

Forbidden inference:

```text
F-polynomial readout equals positive-geometry canonical-form readout
```

---

### QSI-T3 — Hall / Euler / Counting Readouts

Question:

> Does Hall algebra or Euler-characteristic source material support a clean readout-by-counting/invariant pattern relevant to Formation Medium carrier/readout separation?

Desired extraction targets:

```text
Hall product / counting interpretation
Euler characteristic of quiver Grassmannians
readout as invariant of subobject family
limits of counting interpretation
```

Formation contacts:

```text
R7
R9
R11
S2*
```

Status:

```text
SOURCE_NEEDED
```

Forbidden inference:

```text
counting invariant proves physical formation readout
```

---

### QSI-T4 — Downward-Closed / Antichain / Ideal Admissibility

Question:

> Which source material cleanly supports admissibility as ideals, antichains, downward-closed families, or compatible subobject collections?

Desired extraction targets:

```text
order ideals
antichains
closure/downward-closed admissibility
compatibility conditions
```

Formation contacts:

```text
S1
FM1-C2
R1
R2
R9
R12
```

Status:

```text
SOURCE_NEEDED
```

Forbidden inference:

```text
all Formation Medium admissibility is poset admissibility
```

---

### QSI-T5 — Grouped Carriers / Compatibility / Nontransversal Gluing

Question:

> Can grouped-carrier structures and nontransversal compatibility/gluing be source-bound in a way that supports R9-R13 class-matrix comparison?

Desired extraction targets:

```text
grouped carrier or grouped object definition
compatibility/gluing law
nontransversal or overlap condition
factorization over grouped data
```

Formation contacts:

```text
R9
R10
R11
R12
R13
R14
```

Status:

```text
SOURCE_NEEDED
```

Forbidden inference:

```text
grouped carriers solve R14
```

---

### QSI-T6 — Equality-Layer Intrinsicness

Question:

> Can source material distinguish intrinsic equality constraints from degenerative/saturated equality constraints in quiver/grouped-carrier contexts?

Desired extraction targets:

```text
intrinsic equality / defining relation
equality by specialization or saturation
generic inequality vs equality layer
realization-class distinction
```

Formation contacts:

```text
R13
G9
S4
FORMATION_MEDIUM_BULK_READOUT_WITNESS_SCHEMA.md
```

Status:

```text
SOURCE_NEEDED
```

Forbidden inference:

```text
equality-layer status proves substrate realization
```

## 3. Candidate Claim IDs If Sources Are Acquired

Do not use these until actual sources are present and inspected.

Possible ids:

```text
EXT-QUIVER-001
EXT-QUIVER-002
EXT-QUIVER-003
EXT-GROUPED-CARRIER-001
EXT-GROUPED-CARRIER-002
```

Possible check targets:

```text
CHK-QUIVER-SUBOBJECT-CARRIER-SOURCE-COVERAGE
CHK-QUIVER-F-POLYNOMIAL-READOUT-SOURCE-COVERAGE
CHK-HALL-EULER-READOUT-SOURCE-COVERAGE
CHK-GROUPED-CARRIER-EQUALITY-LAYER-SOURCE-COVERAGE
```

Status:

```text
NOT_CREATED
```

## 4. Extraction Discipline

Only extract claims that are:

1. directly stated in a source;
2. scoped to a named mathematical framework;
3. tied to rows or Formation Medium candidate vocabulary only as source contacts;
4. marked `SOURCE_EXTRACTED / NEEDS_FORMALIZATION`;
5. blocked from physical-substrate or engineering inference.

Do not extract:

```text
prior GPT quiver claims
unverified symbolic fragments
physical substrate claims
company strategy claims
Formation Medium claims as source text
```

## 5. R14 Matrix Use

If sources are acquired, populate R14 matrix cells only in this order:

```text
1. source_contacts
2. check_contacts
3. cell_status = SOURCE_EXTRACTED_SOURCE_COVERAGE_GUARDED or weaker
4. promotion_status = BLOCKED
5. notes preserving exact scope
```

Candidate affected rows:

```text
R9 grouped carriers
R10 grouped parameters / weights
R11 grouped factorization
R12 overlap/submodular/compatibility layer
R13 equality-layer status
R14 full matrix deficit
```

R14 remains open even after partial source population.

## 6. Formation Medium Use

Safe use:

```text
quiver/grouped material as mathematical exemplar for S1 carrier/admissibility and possible readout-invariant patterns
```

Unsafe use:

```text
quiver/grouped material as physical substrate
quiver readout as S2* canonical-form proof
quiver equality as S4 proof
grouped carriers as R14 solution
```

## 7. Near-Term Source Review Queue

Priority order:

```text
1. quiver subrepresentations / quiver Grassmannians
2. F-polynomials / cluster characters
3. Hall algebra / Euler characteristic readouts
4. poset ideals / antichain admissibility if needed
5. grouped-carrier compatibility / equality-layer source candidates
```

## 8. Kill Conditions

Reject or rewrite this plan if:

1. quiver material is treated as already source-bound;
2. prior GPT ore is converted directly into source-extracted claims;
3. F-polynomial or Hall/Euler readouts are equated with canonical forms without mapping;
4. quiver/grouped carriers are used as physical substrate evidence;
5. R14 is declared solved from partial source population;
6. G9 equality-layer statuses are assigned to physical routes from quiver analogy;
7. the plan expands the project without concrete source-review questions.

## 9. Non-Promotion Footer

This source-intake plan preserves a large mathematical vein while keeping it quarantined. It does not add source claims, formal results, substrate evidence, engineering readiness, or corporate claims.
