# Formation Medium Candidate Vocabulary

STATUS: GENERATED / OPEN / CANDIDATE  
ARGUMENT_STATUS: ROW_BINDING_SUPPORTED / GENERATED  
CHECK_STATUS: NOT_CHECKED  
SOURCE_STATUS: PARTIAL / SOURCE_COVERAGE_CONTACTS  
PROMOTION_STATUS: BLOCKED  
TARGET_LOCATION: research_ledger/  
DO_NOT_PLACE_IN: SOURCE_EXTRACTED_CLAIMS.json

## 0. Non-Promotion Notice

This file registers candidate vocabulary for the Formation Medium project.

It is not a source-extracted claim, not a theorem, not a formal definition, not a validator result, not a substrate realization, not engineering feasibility, and not a corporate/investor claim.

The labels below are candidate/operator labels only. They are allowed as local organizing vocabulary in generated artifacts, but they are not repo-native theorem terms and must not be placed in `SOURCE_EXTRACTED_CLAIMS.json`.

Inputs:

```text
research_ledger/S2_STAR_S4_DISTINCTNESS_CANDIDATE.md
research_ledger/FORMATION_MEDIUM_LAYER_1_SPEC.md
research_ledger/PREVIOUS_GPT_R1_R14_ROW_BINDING_SYNTHESIS_001.md
research_ledger/FORMATION_MEDIUM_BULK_READOUT_WITNESS_SCHEMA.md
research_ledger/PRIME_LOCAL_CARRIER_CANDIDATE.md
```

## 1. Purpose

The purpose of this artifact is to stabilize language without promoting it.

The prior artifacts repeatedly use:

```text
S1
S2*
S3
S4
```

This registry records what those labels currently mean, which rows/source anchors they touch, and what blocks promotion.

## 2. Vocabulary Entries

### S1 — Exact Carrier + Admissibility

Candidate meaning:

```text
S1 = exact carrier + native admissibility rule
```

Operational reading:

A candidate formation medium must specify native carrier objects/degrees of freedom and the admissibility rules determining valid configurations or transformations.

Row contacts:

```text
R1  unit codimension descent
R2  local move multiplicity
R3  nested-history / linear-extension burden
R9  grouped carriers
R10 grouped parameters / weights
R12 overlap / submodular inequality law
```

Formation contacts:

```text
FM1-C1 native carrier
FM1-C2 native admissibility
```

Current status:

```text
GENERATED / OPEN / CANDIDATE
```

What S1 can safely do:

- organize carrier/admissibility requirements;
- remind the project not to rely on a user-imposed encoding;
- collect row contacts around native objects and valid configurations.

What S1 cannot do:

- identify a physical substrate;
- assert a universal carrier type;
- promote sign vectors, Matryoshkas, grouped carriers, or Prime flip labels as the substrate;
- prove distinctness from ordinary computation.

Promotion blockers:

1. no repo-native carrier vocabulary;
2. carrier shape is presentation-bound across families;
3. no substrate model;
4. R14 class-matrix deficit remains open.

Kill condition:

Reject any use of S1 that treats a specific presentation carrier as substrate-level.

---

### S2* — Intrinsic Canonical Boundary / Recursive Readout

Candidate meaning:

```text
S2* = intrinsic canonical boundary/readout semantics with recursive constraint
```

Operational reading:

The readout should be governed by intrinsic boundary/canonical structure rather than an externally selected output convention.

Row/source contacts:

```text
R7  parabolic residue recursion
R8  root-bundle / local transport context only
POS_GEOM_2017
EXT-SCAT-FORMS-001
EXT-INTO-AMP-2013-001
CHK-S2STAR-READOUT-SOURCE-COVERAGE
```

Formation contacts:

```text
FM1-C4 canonical boundary readout
FORMATION_MEDIUM_BULK_READOUT_WITNESS_SCHEMA.md
```

Current status:

```text
GENERATED / OPEN / PARTIAL_SOURCE_COVERAGE / NOT_THEOREM
```

What S2* can safely do:

- organize canonical boundary/readout requirements;
- cite source-coverage contacts for positive geometry/canonical forms;
- connect R7 residue recursion to readout structure;
- act as a candidate criterion for physical-regime triage.

What S2* cannot do:

- claim a checked theorem;
- claim a physical substrate;
- claim that positive geometry proves Formation Medium;
- claim all readout semantics are exhausted by boundaries;
- use R8 transport as sufficient evidence for canonical readout.

Promotion blockers:

1. source coverage is not theorem status;
2. no S2* formal validator exists;
3. no physical readout/substrate proof exists;
4. R8 cannot promote S2* alone;
5. framework comparisons remain unchecked.

Kill condition:

Reject any use of S2* that treats source-covered canonical-form recursion as a checked substrate criterion.

---

### S3 — Native Local Operations / Transport

Candidate meaning:

```text
S3 = native local operations, operation-class polymorphic
```

Operational reading:

A candidate medium must have native local operations or transport laws, but the operation class may differ by family. The project must not force a universal one-step descent primitive.

Row contacts:

```text
R1 unit codimension descent
R2 local move multiplicity
R8 root-bundle / local transport layer
```

Formation contacts:

```text
FM1-C2 native admissibility/local operations
FM1-C3 settling/local dynamics
G8 transport/readout separation
PREVIOUS_GPT_TRANSPORT_SYMBOL_MINING_SYNTHESIS_001.md
```

Current status:

```text
GENERATED / OPEN / CANDIDATE
```

What S3 can safely do:

- prevent overfitting to one local move type;
- distinguish transport from readout;
- connect R8 transport to engineering triage as a candidate layer;
- preserve the idea that native operations must be internal to the medium.

What S3 cannot do:

- assert a universal operation class;
- promote `V_m` or `J_i` as formal definitions without recovery/checking;
- claim engineering feasibility for couplers/resonators;
- identify transport with canonical readout.

Promotion blockers:

1. no unified operation-class theorem;
2. R2/R8 support is partial and scoped;
3. `V_m` / `J_i` remain generated ore unless separately formalized;
4. transport/readout separation is generated/open.

Kill condition:

Reject any use of S3 that treats hardware coupling, signal routing, or software update rules as Formation Medium behavior by itself.

---

### S4 — Bulk Non-Faithfulness Relative to Readout

Candidate meaning:

```text
S4 = bulk non-faithfulness relative to canonical/readout semantics
```

Operational reading:

The same readout-level structure may correspond to different bulk/geometric/internal realizations. A candidate medium should not require unique reconstruction of the bulk from the readout unless that faithfulness is explicitly intended and source/check supported.

Row/source contacts:

```text
EXT-AMP-PRIME-001
CHK-SAME-BOUNDARY-DIFFERENT-BULK-CAUTION
R13 equality-layer status
R14 class-matrix deficit
FORMATION_MEDIUM_BULK_READOUT_WITNESS_SCHEMA.md
PRIME_LOCAL_CARRIER_CANDIDATE.md
```

Formation contacts:

```text
FM1-C5 bulk/readout non-faithfulness
engineering gate G4 bulk/readout faithfulness status
```

Current status:

```text
GENERATED / OPEN / SOURCE_ANCHORED_CAUTION / NOT_THEOREM
```

What S4 can safely do:

- record the Prime-backed caution that same boundary/log-form does not imply same bulk geometry;
- require engineering routes to declare readout faithfulness status;
- support same_readout/different_bulk witness schema as a generated formal candidate;
- guide R13-style realization-class comparison.

What S4 cannot do:

- generalize Prime to all positive geometries;
- generalize positive-geometric non-faithfulness to physical substrates;
- claim a checked separation theorem;
- assert that all valid Formation Medium routes must be non-faithful.

Promotion blockers:

1. Prime support is source-anchored caution only;
2. no general same-readout/different-bulk formal witness exists;
3. R13 is not theorem status;
4. R14 remains open;
5. engineering readout faithfulness remains unknown for all physical routes.

Kill condition:

Reject any use of S4 that infers physical substrate non-faithfulness from Amplituhedron-Prime alone.

## 3. Cross-Vocabulary Dependencies

The labels are not independent.

```text
S1 supplies candidate carrier/admissibility conditions.
S3 supplies candidate local operation/transport conditions.
S2* supplies candidate canonical readout conditions.
S4 supplies candidate caution about the readout/bulk relation.
```

The current strongest wedge remains:

```text
S2* + S4
```

because it combines intrinsic canonical readout with non-faithful bulk caution.

S1 and S3 are necessary architecture constraints, but not by themselves enough for distinctness.

## 4. Relation to FM1 Constraints

```text
S1  -> FM1-C1, FM1-C2
S2* -> FM1-C4
S3  -> FM1-C2, FM1-C3, G8
S4  -> FM1-C5, engineering G4
```

## 5. Relation to Corporate Goal

The vocabulary is useful for a founder-led corporate technical spine only if it remains disciplined.

Safe corporate framing:

```text
The company is developing a Formation Medium specification whose current candidate vocabulary separates carrier/admissibility, local transport, canonical readout, and bulk/readout faithfulness. Each layer has source contacts, blockers, and validation gates.
```

Unsafe corporate framing:

```text
We have proved a new physical substrate.
We have shown positive geometry is physically realized.
S1-S4 are established laws.
Prime proves the substrate is non-faithful.
```

## 6. Required Next Work

### V1 — Machine-Readable Vocabulary JSON

Create only if needed for validators:

```text
research_ledger/FORMATION_MEDIUM_CANDIDATE_VOCABULARY.json
```

### V2 — R13 Equality-Layer Realization Candidate

Use R13 to refine S4 and realization-class comparisons:

```text
research_ledger/R13_EQUALITY_LAYER_REALIZATION_CANDIDATE.md
```

### V3 — R14 Class Matrix Candidate

Convert R14 into a structured open matrix:

```text
research_ledger/R1_R14_CLASS_MATRIX_CANDIDATE.json
```

### V4 — Corporate Technical Spine Draft

Only after V2/V3 or at least with R14 visible as open:

```text
research_ledger/CORPORATE_TECHNICAL_SPINE_DRAFT.md
```

## 7. Kill Conditions

Reject or rewrite this vocabulary if:

1. S1-S4 are treated as repo-native theorem terms.
2. Any S-label is placed into `SOURCE_EXTRACTED_CLAIMS.json` as if it were source text.
3. S2* is promoted from source coverage to theorem status.
4. S4 is generalized from Prime to physical substrate non-faithfulness.
5. S3 conflates transport with readout.
6. S1 conflates a presentation carrier with substrate carrier.
7. The labels are used in investor/corporate material as established physics.
8. R14 is hidden or ignored.

## 8. Non-Promotion Footer

This candidate vocabulary is a control surface for generated Formation Medium work. It stabilizes language while preserving uncertainty. It does not create source-extracted claims, formal definitions, theorem status, substrate realization, engineering readiness, or corporate proof.
