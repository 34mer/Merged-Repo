# CRBSM Regime Candidate Recovery

STATUS: GENERATED / OPEN / REGIME_CANDIDATE_RECOVERY  
ARGUMENT_STATUS: PRIOR_CONVERSATION_ORE / HANDOFF_SUPPORTED  
CHECK_STATUS: NOT_CHECKED  
SOURCE_STATUS: NOT_SOURCE_EXTRACTED  
PROMOTION_STATUS: BLOCKED  
TARGET_LOCATION: research_ledger/  
DO_NOT_PLACE_IN: SOURCE_EXTRACTED_CLAIMS.json

## 0. Non-Promotion Notice

This artifact recovers the high-salience CRBSM thread that was under-surfaced in earlier engineering-route triage.

CRBSM means:

```text
Chiral Relay Boundary-Sheet Medium
```

This artifact is not a source-extracted claim, not a theorem, not a selected substrate, not engineering feasibility evidence, not a prototype architecture, not IP, and not corporate/investor language.

Machine-readable companion:

```text
research_ledger/PREVIOUS_GPT_CRBSM_REGIME_RECOVERY_001.json
```

Authority/continuity contacts:

```text
exact_work/handoff/00_READ_THIS_FIRST.md
exact_work/handoff/10_MACHINE_STATE.json
exact_work/handoff/20_CONTINUITY_BEARING_NOTES.md
research_ledger/R1_R14_DISCRIMINATOR_LEDGER.md
research_ledger/ROW_FAMILY_COVERAGE_MATRIX.json
```

## 1. Why This Artifact Exists

The prior-conversation corpus contains a large CRBSM thread. Earlier mining passes noticed CRBSM but did not promote it into the current engineering route structure. That was incomplete.

The correct recovery is not:

```text
CRBSM is Route A
CRBSM is the substrate
CRBSM is the company target
```

The correct recovery is:

```text
CRBSM is a generated proxy / fabricable regime-class candidate.
Scattering-native realization is a mandatory comparator, not a dogma and not a default winner.
High-resonance / mesoscopic resonance-native realizations remain open as possible competitors to high-energy scattering.
No substrate commitment has been made.
```

## 2. Definition Recovered from Prior Ore

Prior GPT ore defined CRBSM as:

```text
Chiral Relay Boundary-Sheet Medium
```

Core generated description:

```text
primitive state: oriented codim-1 boundary sheet
medium: scaffolded cellular medium M
native ingredients:
  bipolar shell phase
  anchored boundary sheets
  chiral relay pockets
transport law:
  local quarter-turn / A2-like transport through relay pockets
```

Important symbolic fragments recovered earlier:

```text
sigma_v in {+,-}
Sigma subset M
f_1, ..., f_m
(f_i, f_{i+1}) -> (f_{i+1}, -f_i)
J_i
```

Status:

```text
GENERATED / OPEN / NOT_SOURCE_EXTRACTED / NOT_FORMALIZED
```

## 3. What CRBSM Was Supposed to Realize

The CRBSM thread was generated from extracted constraints around:

```text
C1 native boundary atom
C2 intrinsic family/root-bundle structure
C3 native quarter-turn A2 transport
C4 bulk vs boundary separation
C5 persistent support/channel ledger
C6 scalability to solved top-interaction classes
```

Prior assessment:

```text
CRBSM appears strong on C1-C3 by construction.
CRBSM is weak/unresolved on C5 physically.
```

The hardest unresolved bottleneck is:

```text
persistent support / channel identity / relay-usage ledger
```

This bottleneck is not settled mathematics. It is bridge/specification language and must remain suspect until rederived or physically sourced.

## 4. Commitment Boundary

The handoff files are explicit:

```text
The object is not build CRBSM.
CRBSM is generated candidate only.
Scattering is mandatory comparator and current leading native-realization candidate.
No substrate commitment has been made.
Support-ledger language is suspect bridge language.
```

Therefore CRBSM must not be treated as Route A by default.

It should be tracked as:

```text
Route G / generated proxy candidate
```

or:

```text
CRBSM generated proxy route
```

not as a source family and not as selected engineering target.

## 5. Relation to Existing Routes

### Relation to Route A

Route A currently means:

```text
topoelectrical / topological circuit boundary-readout layer
+ superconducting microwave resonator/coupler transport layer
```

CRBSM is not identical to Route A.

Possible relation:

```text
Route A may become an implementation/proxy technology stack for a CRBSM-like regime if source review supports the boundary/readout and transport/coupling layers.
```

But current status remains:

```text
UNPROVEN / GENERATED / SOURCE_REVIEW_NEEDED
```

### Relation to Route B/C

Route B/C are existing material-platform analog routes:

```text
Route B: topological photonics / microwave-photonic hybrid
Route C: mechanical topological metamaterials
```

CRBSM is broader and more architectural than those. It specifies a regime-class structure; Route B/C may provide components or negative controls, not the regime itself.

### Relation to Route E

Route E is:

```text
scattering / S-matrix regimes
```

This is a mandatory comparator. In the continuity handoff, scattering-native realization is marked as the current leading native-realization candidate, but that should not become dogma. It remains unresolved on controllability, persistence, and apparatus coupling, and high-resonance / mesoscopic resonance-native regimes may beat high-energy scattering if they satisfy the same constraints more natively or more harnessably.

The required comparison is:

```text
CRBSM-like mesoscopic engineered proxy
vs
scattering-native high-energy / law-native realization
vs
high-resonance / mesoscopic resonance-native realization candidates
```

## 6. Corporate / Engineering Interpretation

Safe internal framing:

> CRBSM is a generated proxy/flywheel candidate that may help bootstrap engineering work, while the math remains sovereign and scattering-native realization remains the mandatory native comparator.

Unsafe framing:

```text
We are building CRBSM.
CRBSM is the target.
CRBSM is the selected substrate.
CRBSM is source-backed.
CRBSM is engineering feasible.
```

A better corporate sequence is:

```text
1. preserve CRBSM as generated proxy candidate
2. preserve scattering-native as mandatory comparator, not default winner
3. preserve high-resonance / mesoscopic resonance-native candidates as open competitors
4. compare all surviving candidates against C1-C6 / FM gates
5. only then decide whether Route A/B/C technologies are implementation proxies
6. keep migration path explicit from day one
```

## 7. Suggested Matrix / Route Updates

The engineering gate matrix should eventually include CRBSM as a generated proxy route:

```text
ROUTE-G: CRBSM / Chiral Relay Boundary-Sheet Medium
role: generated_proxy_candidate / fabricable_proxy_candidate
```

Initial statuses should be conservative:

```text
G1 native carrier: GENERATED_BY_CONSTRUCTION
G2 native admissibility/local ops: GENERATED_BY_CONSTRUCTION
G3 intrinsic boundary/readout: GENERATED_BY_CONSTRUCTION
G4 bulk/readout faithfulness: UNKNOWN
G5 native settling/formation dynamics: OPEN
G6 lab/corporate tractability: POSSIBLE_BOOTSTRAP_PROXY / NOT_REVIEWED
G7 failure interface: CAN_BE_DEFINED
G8 transport/readout separation: CENTRAL_OPEN_GATE
G9 equality-layer intrinsicness: UNKNOWN
```

Negative-control risks:

```text
NC-006 mere boundary-mode visualization
NC-007 hardware routing without intrinsic readout
NC-008 readout-faithfulness assumption
bridge language treated as theorem
constructing-by-assumption
```

## 8. Required Comparator Work

The next disciplined work is not more CRBSM elaboration by itself.

Required comparison artifact:

```text
CRBSM_SCATTERING_NATIVE_COMPARATOR.md
```

Purpose:

Compare:

```text
CRBSM generated mesoscopic/fabricable proxy
vs
scattering-native realization candidate
vs
high-resonance / mesoscopic resonance-native realization candidates
```

against:

```text
C1 native boundary atom
C2 intrinsic family/root-bundle structure
C3 native quarter-turn / A2 transport
C4 bulk vs boundary separation
C5 persistent support/channel ledger
C6 scalability to solved top interaction classes
G1-G9 Formation Medium gates
```

Status must remain:

```text
GENERATED / OPEN / NOT_SOURCE_EXTRACTED / NOT_SUBSTRATE_SELECTION
```

## 9. Required Source/Review Questions

### CRBSM-Q1 — Physical Mechanism for Relay Ledger

Question:

> Is there any physically plausible mechanism for slow, conserved, or metastably persistent relay-usage variables bound to relay pockets?

Status:

```text
OPEN / HARD BOTTLENECK
```

### CRBSM-Q2 — Boundary Sheet Medium Physics

Question:

> Can oriented codim-1 boundary sheets in any mesoscopic medium be native state variables rather than designer-imposed interfaces?

Status:

```text
OPEN / SOURCE_REVIEW_NEEDED
```

### CRBSM-Q3 — Quarter-Turn Transport

Question:

> Can local relay pockets implement a native quarter-turn law such as `(f_i, f_{i+1}) -> (f_{i+1}, -f_i)` without merely simulating it?

Status:

```text
OPEN / SOURCE_REVIEW_NEEDED
```

### CRBSM-Q4 — Scattering Comparator

Question:

> Does scattering-native physics satisfy the same extracted constraints more natively than CRBSM, and can it be harnessed/apparatus-coupled?

Status:

```text
OPEN / MANDATORY COMPARATOR / NOT_DEFAULT_WINNER
```

### CRBSM-Q5 — High-Resonance Comparator

Question:

> Can high-resonance / mesoscopic resonance-native regimes satisfy the extracted constraints more natively or more harnessably than high-energy scattering?

Status:

```text
OPEN / COMPARATOR_NEEDED / NOT_YET_SCOPED
```

## 10. Kill Conditions

Reject or demote CRBSM if:

1. it becomes the default substrate target without comparison;
2. persistent support/relay ledger remains constructed-by-assumption;
3. boundary sheets are externally imposed interfaces rather than native state variables;
4. quarter-turn transport is only simulated or routed;
5. Route A/B/C technologies cannot plausibly implement CRBSM-like primitives;
6. comparator work shows CRBSM is only a weak shadow of a more native scattering or high-resonance object;
7. CRBSM language enters corporate/investor material as selected substrate;
8. CRBSM is treated as source authority.

## 11. Non-Promotion Footer

CRBSM is recovered here because it is salient and structurally important. It remains generated-only, not the object, not source-backed, not selected, not engineering-ready, and not investor-ready.
