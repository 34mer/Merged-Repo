# SN-C3 Static-Gated Source Review

STATUS: GENERATED / EXTERNAL_WEB_REVIEWED / STATIC_GATED_REVIEW / NOT_SOURCE_EXTRACTED / NOT_SUBSTRATE_SELECTION / NOT_ENGINEERING_READY  
PARENT PRIORITY: PRI-002  
CANDIDATE CLASS: SN-C3_PHYSICAL_SCATTERING_MATRIX_TOPOLOGY  
ARTIFACT ROLES: SOURCE_REVIEW / COMPARATOR / STATIC_GEOMETRY_GATE / NEGATIVE_CONTROL / PHYSICAL_REGIME_TRIAGE  
DO_NOT_PLACE_IN: SOURCE_EXTRACTED_CLAIMS.json

## 0. Non-Promotion Notice

This SN-C3 static-gated review is external-web-reviewed only. It does not ingest sources into the repo corpus, extract source claims, validate scattering-native physics, defeat Ising/high-resonance comparison, select a substrate, prove a settling law, or establish engineering readiness.

## 1. Question

Do physical scattering-matrix topology / phase-singularity observables touch the pre-spacetime static spine?

Not:

```text
Are they interesting photonics?
Are they topological?
Are they experimentally real?
```

The actual test is:

```text
Do they pass SG0–SG4?
```

## 2. Source Basis

### SN-SRC-009

```text
Chen and Liu
Continuous Lines of Topological Singularities in Metasurface Scattering Matrices: From Nodal to Exceptional
ACS Photonics 2025
DOI 10.1021/acsphotonics.5c00575
```

External review note:

```text
Reports topological singularities / eigenvalue degeneracies in scattering matrices of open periodic photonic systems, protected by mirror symmetry, transforming from nodal to exceptional structures with loss, and appearing as vortex lines in cross-polarization scattering phase. Includes numerical and experimental metasurface demonstrations.
```

Status:

```text
EXTERNAL_WEB_REVIEWED_ONLY / NOT_SOURCE_EXTRACTED
```

### SN-SRC-010

```text
Ermolaev et al.
Topological phase singularities in atomically thin high-refractive-index materials
Nature Communications 2022
DOI 10.1038/s41467-022-29716-4
```

External review note:

```text
Reports topological phase singularities in reflection of atomically thin high-refractive-index materials coupled to Fabry–Perot resonators; near reflection zeros, phase becomes singular with topological charge, and the effect is demonstrated for sensing / flat optics.
```

Status:

```text
EXTERNAL_WEB_REVIEWED_ONLY / NOT_SOURCE_EXTRACTED
```

## 3. Critical-Review Prework

Hard bottleneck attacked:

```text
whether physical scattering-matrix observables can honestly touch the pre-spacetime static residue spine, instead of winning by S-matrix / positive-geometry proximity or photonic topology language
```

Thin-stick risks:

```text
thin-stick risk: scattering-matrix eigenvalue topology treated as canonical readout
phase vortices treated as ordered residue signs
reflection zeros treated as Gr_+(0,n) terminal residues
metasurface or 2D-material experiments treated as substrate evidence
```

Negative-progress risk:

```text
This could become impressive photonics review with no actual SG0–SG4 contact.
```

Why this is not convenient drift:

```text
It directly tests whether SN-C3 survives the static-spine correction or should be demoted to negative-control comparator.
```

## 4. PG1–PG8 Assessment

| Gate | Assessment | Reason | Failure mode |
|---|---|---|---|
| PG1 native carrier | PARTIAL | Physical wave carriers exist in photonic/metasurface systems. | Carrier is ordinary electromagnetic response, not Formation Medium substrate. |
| PG2 native admissibility | PARTIAL_TO_OPEN | Symmetry protection, scattering-matrix topology, reflection zeros, and topological charge impose constraints in device/parameter space. | Constraints are photonic/material design constraints, not native positive-geometric admissibility. |
| PG3 relay/transport | WEAK_TO_PARTIAL | Scattering response and phase topology exist, but relay/transport semantics are not defined. | Observable phase response replaces actual formation transport. |
| PG4 persistent support identity | OPEN_WEAK | Singularity lines and phase singularities have identifiable structure in parameter space. | Identity is parameter-tracked by the experimenter. |
| PG5 intrinsic readout | CANDIDATE_BUT_WEAK | Scattering phase vortices and topological charge are physical observables. | External measurement phase is confused with intrinsic canonical form/readout. |
| PG6 bulk/readout separation | OPEN | Scattering matrices may compress material/bulk structure into boundary response. | Readout remains a device response, not non-faithful geometric projection. |
| PG7 harnessability | PARTIAL | Metasurface and atomically thin material platforms are experimentally accessible. | Harnessability becomes photonic device engineering. |
| PG8 negative-control clarity | STRONG | SN-C3 can cleanly fail SG0–SG4 while remaining interesting physics. | Negative control is ignored because the sources sound geometrically suggestive. |

## 5. SG0–SG4 Static-Spine Assessment

### SG0 — Gr_+(0,n) Terminal Atom

Assessment:

```text
FAIL_OR_UNSUPPORTED
```

Reason:

```text
Reflection zeros / scattering zeros are tempting analogues, but they are zeros of optical response functions or scattering operators, not terminal residue atoms with Omega = 1.
```

Required before upgrade:

```text
A precise map from response zero to terminal residue atom preserving canonical-form semantics, not merely zero amplitude or perfect absorption.
```

### SG1 — Gr_+(1,n) Static Simplex Tower

Assessment:

```text
FAIL_OR_UNSUPPORTED
```

Reason:

```text
The sources do not expose a static simplex residue tower with recursive facets Gr_+(1,n-1). They expose parameter-space topology of scattering / phase response.
```

Required before upgrade:

```text
A source-backed or formal map from scattering observable strata to recursive Gr_+(1,n) boundary tower.
```

### SG2 — Ordered Residue Sign

Assessment:

```text
OPEN_BUT_WEAK
```

Reason:

```text
Phase winding / topological charge has sign and loop-order flavor, but it is not ordered residue-route sign/coherence. Treating phase vortex charge as residue sign would be thin-stick leverage.
```

Required before upgrade:

```text
An explicit comparison showing whether phase winding order implements, represents, or only mimics ordered residue signs.
```

### SG3 — Product Static Block Gr_+(1,2)^m

Assessment:

```text
FAIL_OR_UNSUPPORTED
```

Reason:

```text
The sources do not identify local chart variables as products of Gr_+(1,2) interval blocks. Coupled-mode or scattering parameters are not automatically static interval factors.
```

Required before upgrade:

```text
A finite model mapping scattering parameters or mode couplings to product static blocks with residue semantics.
```

### SG4 — Mutation Transport

Assessment:

```text
FAIL_OR_UNSUPPORTED
```

Reason:

```text
Changing frequency, momentum, loss, material, or device geometry is not mutation as transported static reflection. The reviewed sources do not show transport of static residue data through atlas transitions.
```

Required before upgrade:

```text
A defined mutation-like transition preserving SG0–SG3 data, not merely tunable device parameters or exceptional/nodal transition.
```

## 6. Red-Team Conclusion

Classification:

```text
USEFUL_NEGATIVE_CONTROL_AND_WEAK_OBSERVABLE_COMPARATOR
```

Summary:

```text
SN-C3 has real physical observables and is useful for testing whether scattering-matrix topology can function as readout. But under SG0–SG4 it currently fails the core pre-spacetime static-spine gates.
```

Continue or demote:

```text
CONTINUE_AS_NEGATIVE_CONTROL_AND_OBSERVABLE_COMPARATOR_ONLY
```

Recommended next:

```text
Compare SN-C3 against PRI-005 Ising machines on SG0–SG4. Scattering observables may have stronger physical phase/topology readout, while Ising machines may have stronger combinatorial settling but also likely fail canonical residue semantics.
```

## 7. Non-Promotion Footer

This SN-C3 static-gated review is external-web-reviewed only. It does not ingest sources into the repo corpus, extract source claims, validate scattering-native physics, defeat Ising/high-resonance comparison, select a substrate, prove a settling law, or establish engineering readiness.
