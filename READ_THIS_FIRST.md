# Read This First

This is the merged ProjectX repository.

Authority order:

1. Code and typed records in `pg_pipeline/` define the live scientific lifecycle.
2. ProjectX runtime state in `projectx/kernel/state/` records continuity and supervision.
3. Handoff and Lean files in `exact_work/` preserve mathematical authority and formal status.
4. `archive/` preserves source identity but is not live operational code.

Current live slice:

- family: `grassmannian_small`
- lifecycle: generate, normalize, propose, check, stress, substrate, review
- outer supervisor: `projectx.control.pipeline_runner`

Input scope:

- The only source inputs are `ProjectX_extracted.zip`, `research-ledger-v1-clean-main.zip`, and `111.txt` through `555.txt`.
- Nested bundle references and any separate GitHub/final-repo source are intentionally ignored.
